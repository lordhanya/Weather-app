{
  "name": "weathersphere",
  "version": "1.0.0",
  "description": "Explore weather on an interactive 3D globe — no API key, no backend",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}
